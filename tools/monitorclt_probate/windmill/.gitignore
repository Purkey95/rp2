# Staged by sync.sh from the tool directory one level up; the copies here are
# build output, and committing them would create a second matcher to keep in step.
f/monitorclt/crossref.py
f/monitorclt/load_run.py

# Generated by sync.sh from ../match_rules.json, same reason.
f/monitorclt/probate_match_rules.variable.yaml

# Database credentials. Copy resources.example/ to f/monitorclt/, fill in, push.
# Never commit a filled-in resource spec.
f/monitorclt/*.resource.yaml
